import React from "react";
import { CommentBox } from "../components";

const Form: React.FC = () => {
  return (
    <section data-testid="form-page">
      <CommentBox />
    </section>
  );
};

export default Form;
